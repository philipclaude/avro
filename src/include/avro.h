#ifndef AVRO_API_AVRO_H_
#define AVRO_API_AVRO_H_

#define AVRO_SUCCESS 0
#define AVRO_ERROR -1
#define AVRO_WARNING 1
#define AVRO_NOT_CONFORMING 2

namespace avro
{

/*
template<typename type> class Mesh;
class Model;
template<typename type> class Domain;

namespace numerics
{
template<typename type> class DiscreteMetric;
}

template<typename type>
int adapt( AdaptationProblem<type>& problem );

template<typename type>
void write_mesh( const std::string& filename , const Topology<type>& topology , const numerics::DiscreteField* metric=NULL );

template<typename type>
void read_mesh( const std::string& filename , Mesh<type>& mesh , const Model* model=NULL );
*/

} // avro

#endif
